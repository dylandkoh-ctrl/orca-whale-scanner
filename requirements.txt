# Orca — World Cup Whale Scanner
streamlit>=1.50
httpx>=0.27
pandas>=2.2
python-dateutil>=2.9
tzdata>=2024.1          # IANA tz database for zoneinfo (cloud servers run in UTC)
python-dotenv>=1.0      # load Telegram secrets from .env (wallet_watchlist.py)
# optional (added now, wired in later phases)
streamlit-autorefresh>=1.0.1
APScheduler>=3.10
